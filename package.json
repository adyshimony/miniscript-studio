{
  "name": "miniscript-compiler",
  "version": "1.0.0",
  "description": "WebAssembly-powered miniscript compiler for Bitcoin scripts",
  "scripts": {
    "build": "wasm-pack build --target web --out-dir pkg"
  },
  "devDependencies": {
    "wasm-pack": "^0.12.1"
  }
}